#include <stdio.h>
int main() {
    int number;

 int x;
    printf("Enter the value of x: ");
    scanf("%d", &x);
    
    if (x>0)
    {
        printf ( "positive number");

    }

    return 0;
}